# IoTiz - 구구시스템 RTK GPS 디바이스 LED 동작 규격

---

## 1. LED 하드웨어 구성

### 1.1 LED 배치

| LED ID | 기능 | GPIO 핀 |
|--------|------|---------|
| LED 1 | GSM/NTRIP 상태 표시 | GPIOC 1-2 (R-G) |
| LED 2 | GPS 상태 표시 | GPIOC 3-4 (R-G) |
| LED 3 | LoRa 상태 표시 | GPIOB 13-14 (R-G) |

### 1.2 LED 색상 정의

| 색상 | 값 | GPIO 상태 | 일반적 의미 |
|------|----|-----------| ------------|
| OFF (꺼짐) | 0 | R=HIGH, G=HIGH | 기능 비활성화 |
| RED (빨강) | 1 | R=LOW, G=HIGH | 에러/실패/연결 끊김 |
| YELLOW (노랑) | 2 | R=LOW, G=LOW | 대기/진행 중/중간 상태 |
| GREEN (초록) | 3 | R=HIGH, G=LOW | 정상/성공/연결됨 |

### 1.3 점멸 패턴

| 패턴 | 설명 | 용도 |
|------|------|------|
| 점등 (Solid) | 계속 켜져 있음 | 상태 유지 표시 |
| 점멸 (Blink) | 주기적으로 깜빡임 (~1Hz) | 데이터 송수신/동작 중 표시 |

---

## 2. BASE 모드 LED 동작

### 2.1 LED 1 - GSM/NTRIP 상태 (BASE)

#### 2.1.1 수동좌표 사용 설정 시 (NTRIP 미사용)

| 단계 | 상태 | LED 색상 | 점멸 | 설명 |
|------|------|----------|------|------|
| 1 | 초기화 | OFF | - | NTRIP 기능 비활성화 |

> **참고:** 수동좌표 모드에서는 NTRIP을 사용하지 않으므로 LED 1은 항상 꺼져 있습니다.

#### 2.1.2 수동좌표 미사용 설정 시 (NTRIP 사용 - Auto Averaging)

| 단계 | 상태 | LED 색상 | 점멸 | 설명 |
|------|------|----------|------|------|
| 1 | 디바이스 구동 | RED | 점등 | GSM 모듈 초기화 시작 |
| 2 | GSM 초기화 중 | RED | 점멸 | GSM 데이터 수신마다 토글 |
| 3 | NTRIP 연결 시도 | YELLOW | 점등 | TCP 소켓 연결 시도 중 |
| 4a | 연결 성공 | GREEN | 점등 | NTRIP 서버 연결 완료 |
| 4b | 연결 실패 | RED | 점등 | TCP 연결 실패, 재시도 대기 |
| 5 | 데이터 수신 중 | GREEN | 점등 | 보정 데이터 정상 수신 |
| 6 | 데이터 타임아웃 (1~2회) | YELLOW | 점등 | 데이터 수신 대기 중 |
| 7 | 데이터 타임아웃 (3회) | RED | 점등 | 재연결 시도 시작 |
| 8 | 재연결 성공 | GREEN | 점등 | 연결 복구 완료 |
| 9 | Averaging 완료 후 | OFF | - | NTRIP 종료, Base Mode 전환 |

#### 2.1.3 NTRIP 연결 상태 전이도

```
┌──────────────────────────────────────────────────────────────────────┐
│                        NTRIP 연결 흐름 (BASE)                         │
└──────────────────────────────────────────────────────────────────────┘

     ┌─────────────┐
     │  GSM 시작   │
     │  LED: RED   │
     └──────┬──────┘
            │
            ▼
     ┌─────────────┐
     │ NTRIP 시작  │
     │ LED: YELLOW │
     └──────┬──────┘
            │
            ▼
      ┌───────────┐     연결 실패      ┌───────────┐
      │ TCP 연결  │ ─────────────────▶ │ LED: RED  │
      │   시도    │                    │  재시도   │──┐
      └─────┬─────┘                    └───────────┘  │
            │ 연결 성공                       ▲        │
            ▼                                 │        │
      ┌───────────┐                          │        │
      │LED: GREEN │◀─────────────────────────┴────────┘
      │데이터 수신│
      └─────┬─────┘
            │
            │ 타임아웃 발생
            ▼
      ┌───────────┐
      │LED: YELLOW│  타임아웃 1~2회: YELLOW 유지
      │  대기 중  │
      └─────┬─────┘
            │ 타임아웃 3회 연속
            ▼
      ┌───────────┐
      │ LED: RED  │  재연결 프로세스 시작
      │  재연결   │
      └─────┬─────┘
            │ 재연결 성공
            ▼
      ┌───────────┐
      │LED: GREEN │
      │ 연결 복구 │
      └───────────┘
```

#### 2.1.4 GSM/LTE 이벤트별 LED 동작

| 이벤트 | LED 색상 | 설명 |
|--------|----------|------|
| GSM_EVT_INIT_FAIL | RED | GSM 모듈 초기화 실패 |
| GSM_EVT_TCP_CLOSED | RED | TCP 연결 종료됨 |
| GSM_EVT_PDP_DEACT | YELLOW | PDP 컨텍스트 비활성화 (네트워크 문제) |

---

### 2.2 LED 2 - GPS 상태 (BASE)

| GPS Fix 상태 | Fix 값 | LED 색상 | 점멸 | 설명 |
|--------------|--------|----------|------|------|
| No Fix | 0 | RED | 점멸 | GPS 신호 없음 |
| GPS Fix (SPS) | 1 | RED | 점멸 | 단독 측위 |
| DGPS | 2 | RED | 점멸 | 차분 GPS |
| RTK Fixed | 4 | **YELLOW** | 점멸 | RTK 고정해 (⚠️ BASE는 YELLOW) |
| RTK Float | 5 | YELLOW | 점멸 | RTK 부동해 |
| Manual Position | - | GREEN | 점멸 | 수동 좌표 설정됨 |
| HDOP ≥ 99.0 | - | GREEN | 점멸 | Averaging 완료 상태 |

> **중요:** BASE 모드에서는 RTK Fixed 상태여도 LED가 **YELLOW**입니다.
> 이는 ROVER와 구분하기 위한 의도적인 설계입니다.

#### 2.2.1 BASE 모드 GPS LED 상태도

```
┌──────────────────────────────────────────────────────────────────────┐
│                       GPS LED 상태 (BASE 모드)                        │
└──────────────────────────────────────────────────────────────────────┘

     GPS 데이터 수신 (1Hz마다 토글)
              │
              ▼
     ┌─────────────────┐
     │  Fix Quality?   │
     └────────┬────────┘
              │
    ┌─────────┼─────────┬─────────────┐
    │         │         │             │
    ▼         ▼         ▼             ▼
┌───────┐ ┌───────┐ ┌───────┐   ┌───────────┐
│0,1,2  │ │  4,5  │ │Manual │   │HDOP ≥ 99.0│
│No Fix │ │ RTK   │ │ Pos   │   │(Averaging)│
│ DGPS  │ │       │ │       │   │           │
└───┬───┘ └───┬───┘ └───┬───┘   └─────┬─────┘
    │         │         │             │
    ▼         ▼         ▼             ▼
┌───────┐ ┌───────┐ ┌───────┐   ┌───────────┐
│ RED   │ │YELLOW │ │ GREEN │   │   GREEN   │
│ 점멸  │ │ 점멸  │ │ 점멸  │   │   점멸    │
└───────┘ └───────┘ └───────┘   └───────────┘
```

---

### 2.3 LED 3 - LoRa 상태 (BASE)

| 상태 | LED 색상 | 점멸 | 설명 |
|------|----------|------|------|
| 초기화 성공 | GREEN | 점등 | LoRa 모듈 정상 시작 |
| 초기화 실패 | RED | 점등 | LoRa 모듈 시작 실패 |
| RTCM 데이터 송신 | GREEN | 점멸 | TX마다 토글 (데이터 전송 표시) |
| 모듈 정지 | OFF | - | LoRa 기능 비활성화 |

> **참고:** BASE 모드에서 LED 3은 **송신(TX)** 시 점멸합니다.

---

## 3. ROVER 모드 LED 동작

### 3.1 LED 1 - GSM/NTRIP 상태 (ROVER)

> ROVER 모드의 NTRIP LED 동작은 BASE 모드와 **동일**합니다.
> 2.1절 참조

---

### 3.2 LED 2 - GPS 상태 (ROVER)

| GPS Fix 상태 | Fix 값 | LED 색상 | 점멸 | 설명 |
|--------------|--------|----------|------|------|
| No Fix | 0 | RED | 점멸 | GPS 신호 없음 |
| GPS Fix (SPS) | 1 | RED | 점멸 | 단독 측위 |
| DGPS | 2 | RED | 점멸 | 차분 GPS |
| RTK Fixed | 4 | **GREEN** | 점멸 | RTK 고정해 (⚠️ ROVER는 GREEN) |
| RTK Float | 5 | YELLOW | 점멸 | RTK 부동해 |
| Manual Position | - | GREEN | 점멸 | 수동 좌표 설정됨 |

> **중요:** ROVER 모드에서는 RTK Fixed 상태일 때 LED가 **GREEN**입니다.
> 이것이 BASE와 ROVER의 가장 큰 차이점입니다.

#### 3.2.1 ROVER 모드 GPS LED 상태도

```
┌──────────────────────────────────────────────────────────────────────┐
│                      GPS LED 상태 (ROVER 모드)                        │
└──────────────────────────────────────────────────────────────────────┘

     GPS 데이터 수신 (1Hz마다 토글)
              │
              ▼
     ┌─────────────────┐
     │  Fix Quality?   │
     └────────┬────────┘
              │
    ┌─────────┼─────────┬─────────────┐
    │         │         │             │
    ▼         ▼         ▼             ▼
┌───────┐ ┌───────┐ ┌───────┐   ┌───────────┐
│0,1,2  │ │   5   │ │   4   │   │  Manual   │
│No Fix │ │ RTK   │ │ RTK   │   │   Pos     │
│ DGPS  │ │ Float │ │ Fixed │   │           │
└───┬───┘ └───┬───┘ └───┬───┘   └─────┬─────┘
    │         │         │             │
    ▼         ▼         ▼             ▼
┌───────┐ ┌───────┐ ┌───────┐   ┌───────────┐
│ RED   │ │YELLOW │ │ GREEN │   │   GREEN   │
│ 점멸  │ │ 점멸  │ │ 점멸  │   │   점멸    │
└───────┘ └───────┘ └───────┘   └───────────┘
```

---

### 3.3 LED 3 - LoRa 상태 (ROVER)

| 상태 | LED 색상 | 점멸 | 설명 |
|------|----------|------|------|
| 초기화 성공 | GREEN | 점등 | LoRa 모듈 정상 시작 |
| 초기화 실패 | RED | 점등 | LoRa 모듈 시작 실패 |
| RTCM 데이터 수신 | GREEN | 점멸 | RX마다 토글 (데이터 수신 표시) |
| 모듈 정지 | OFF | - | LoRa 기능 비활성화 |

> **참고:** ROVER 모드에서 LED 3은 **수신(RX)** 시 점멸합니다.

---

## 4. BASE vs ROVER 모드 비교

### 4.1 LED 동작 차이점 요약

| 항목 | BASE 모드 | ROVER 모드 |
|------|-----------|------------|
| **LED 2: RTK Fixed 시** | YELLOW (점멸) | GREEN (점멸) |
| **LED 3: 토글 시점** | TX (송신 시) | RX (수신 시) |
| **NTRIP 용도** | Averaging용 보정데이터 수신 | RTK 보정데이터 수신 |
| **Averaging 완료 후 LED 1** | OFF (NTRIP 종료) | 계속 동작 |

### 4.2 시각적 비교표

```
┌──────────────────────────────────────────────────────────────────────┐
│                    RTK Fixed 상태에서의 LED 비교                      │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   BASE 모드:          │          ROVER 모드:                         │
│                       │                                              │
│   LED 1: (상황에 따라)│          LED 1: (상황에 따라)                │
│   LED 2: YELLOW 점멸  │          LED 2: GREEN 점멸  ◀── 차이점!     │
│   LED 3: GREEN 점멸   │          LED 3: GREEN 점멸                   │
│          (TX 시)      │                 (RX 시)                      │
│                       │                                              │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 5. BLE 상태 알림 (Base Notify)

### 5.1 수동좌표 미사용 설정 시 (Auto Averaging)

디바이스 구동 후 BLE를 통해 주기적으로 상태를 전송합니다.

| 단계 | 알림 내용 | 형식 | 설명 |
|------|-----------|------|------|
| 1 | NTRIP 접속 시도 | `"x,x\n"` | 10초마다 전송 |
| | | 첫번째 숫자: 접속 상태 | 0=접속중, 1=성공, 2=실패 |
| | | 두번째 숫자: GPS Fix | GPS Fix Quality 값 |
| 2 | Averaging 시작 | `"Start Averaging x%"` | Fix Mode 진입 후 시작 (50초 소요) |
| 3 | Averaging 완료 | `"Start Base use ntrip"` | Base Mode 동작 시작 |

#### 5.1.1 접속 상태 값과 LED 1 색상 매핑

| 접속 상태 값 | 의미 | LED 1 색상 |
|--------------|------|------------|
| 0 | 접속 중 | YELLOW |
| 1 | 접속 성공 | GREEN |
| 2 | 접속 실패/없음 | RED 또는 OFF |

### 5.2 수동좌표 사용 설정 시

| 단계 | 알림 내용 | 설명 |
|------|-----------|------|
| 1 | `"Start Base manual"` | Base Mode 수동 시작 |
| 2 | 설정 변경 필요 시 | RS 명령어로 디바이스 재시작 |

---

## 6. 전체 LED 상태 요약표

### 6.1 LED 1 (GSM/NTRIP)

| 색상 | 상태 | 점멸 |
|------|------|------|
| OFF | NTRIP 미사용 / 종료됨 | - |
| RED (점등) | 초기화 중 | - |
| RED (점멸) | GSM 데이터 수신 중 | 데이터마다 |
| RED (점등) | 연결 실패 / 재연결 중 | - |
| YELLOW | 연결 시도 중 / 타임아웃 | - |
| GREEN | 연결 성공 / 데이터 수신 중 | - |

### 6.2 LED 2 (GPS)

| 색상 | BASE 모드 | ROVER 모드 | 점멸 |
|------|-----------|------------|------|
| OFF | GPS 미동작 | GPS 미동작 | - |
| RED | Fix 없음/DGPS | Fix 없음/DGPS | 1Hz |
| YELLOW | RTK Float/Fixed | RTK Float | 1Hz |
| GREEN | Manual Pos/Averaging완료 | RTK Fixed/Manual Pos | 1Hz |

### 6.3 LED 3 (LoRa)

| 색상 | BASE 모드 | ROVER 모드 | 점멸 |
|------|-----------|------------|------|
| OFF | LoRa 정지 | LoRa 정지 | - |
| RED | 초기화 실패 | 초기화 실패 | - |
| GREEN (점등) | 초기화 성공 | 초기화 성공 | - |
| GREEN (점멸) | RTCM TX 중 | RTCM RX 중 | TX/RX마다 |

---

## 7. 트러블슈팅 가이드 (GSM/NTRIP 중심)

### 7.1 GSM/LTE 초기화 단계별 LED 상태 및 문제 진단

GSM 모듈은 여러 단계를 거쳐 초기화됩니다. 각 단계에서 실패 시 LED 1이 RED로 유지됩니다.

#### 7.1.1 GSM 초기화 흐름도

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         GSM/LTE 초기화 단계                               │
└──────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐
    │  전원 ON        │  LED 1: RED 점등
    │  GSM 태스크 시작│
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 1. AT 통신 확인 │ ────────────▶ │ LED 1: RED 지속                 │
    │    (AT → OK)    │               │ → 모뎀 하드웨어 또는 UART 문제  │
    └────────┬────────┘               └─────────────────────────────────┘
             │ 성공
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 2. 에코 OFF     │ ────────────▶ │ LED 1: RED 지속                 │
    │    (ATE0)       │               │ → AT 커맨드 응답 지연           │
    └────────┬────────┘               └─────────────────────────────────┘
             │ 성공
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 3. SIM 카드     │ ────────────▶ │ LED 1: RED 지속                 │
    │    (AT+CPIN?)   │               │ → SIM 카드 없음/잠김/손상       │
    └────────┬────────┘               └─────────────────────────────────┘
             │ 성공 (READY)
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 4. APN 설정     │ ────────────▶ │ LED 1: RED 지속                 │
    │  (AT+QICSGP)    │               │ → APN 정보 오류                 │
    └────────┬────────┘               └─────────────────────────────────┘
             │ 성공
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 5. 네트워크등록 │ ────────────▶ │ LED 1: RED 지속                 │
    │   (AT+CREG?)    │               │ → 통신사 서비스 불가/음영 지역  │
    └────────┬────────┘               └─────────────────────────────────┘
             │ 성공
             ▼
    ┌─────────────────┐
    │ GSM_EVT_INIT_OK │  LED 1: RED → YELLOW (NTRIP 연결 시도)
    │   초기화 완료   │
    └─────────────────┘
```

#### 7.1.2 GSM 초기화 실패 상세 진단표

| 실패 단계 | 에러 메시지 (로그) | LED 상태 | 원인 | 해결 방법 |
|-----------|-------------------|----------|------|-----------|
| AT 통신 | "AT 통신 실패" | RED 지속 | 모뎀 전원 OFF, UART 연결 불량 | 전원 확인, 커넥터 점검 |
| ATE0 | "ATE0 설정 실패" | RED 지속 | 모뎀 응답 지연 | 전원 재시작 |
| CPIN | "AT+CPIN? 실패" | RED 지속 | SIM 미삽입, PIN 잠금 | SIM 카드 재삽입, PIN 해제 |
| APN 설정 | "APN 설정 실패" | RED 지속 | APN 파라미터 오류 | BLE로 APN 재설정 |
| APN 확인 | "APN 확인 실패" | RED 지속 | APN 활성화 안됨 | 통신사 APN 정보 확인 |
| 네트워크 등록 | "네트워크 등록 실패" | RED 지속 | 음영 지역, 통신사 문제 | 위치 이동, 안테나 확인 |
| Keep-alive | "keep-alive 설정 실패" | RED 지속 | 모뎀 설정 오류 | 펌웨어 업데이트 |

#### 7.1.3 GSM 초기화 재시도 로직

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      GSM 초기화 재시도 메커니즘                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   소프트웨어 재시도: 최대 3회                                           │
│   ├── 1차 실패 → 즉시 재시도                                           │
│   ├── 2차 실패 → 즉시 재시도                                           │
│   └── 3차 실패 → 하드웨어 리셋 시도                                    │
│                                                                         │
│   하드웨어 리셋 후:                                                     │
│   ├── 리셋 성공 → 초기화 재시작                                        │
│   └── 리셋 후에도 실패 → GSM_EVT_INIT_FAIL 발생                        │
│                          LED 1: RED 지속 유지                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 7.2 NTRIP 연결 단계별 LED 상태 및 문제 진단

#### 7.2.1 NTRIP 연결 흐름도

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         NTRIP 연결 단계                                  │
└──────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐
    │ NTRIP 태스크    │  LED 1: YELLOW
    │ 시작            │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 1. GGA 큐 생성  │ ────────────▶ │ LED 1: RED                      │
    │                 │  (3회 재시도) │ → 메모리 부족                   │
    └────────┬────────┘               │ → 태스크 종료                   │
             │ 성공                   └─────────────────────────────────┘
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 2. TCP 소켓     │ ────────────▶ │ LED 1: RED                      │
    │    생성         │  (3회 재시도) │ → GSM 모듈 문제                 │
    └────────┬────────┘               └─────────────────────────────────┘
             │ 성공
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 3. TCP 연결     │ ────────────▶ │ LED 1: RED                      │
    │ (서버:포트)     │  (3회 재시도) │ → 서버 주소/포트 오류           │
    │                 │               │ → 네트워크 불안정               │
    └────────┬────────┘               └─────────────────────────────────┘
             │ 성공
             ▼
    ┌─────────────────┐     실패      ┌─────────────────────────────────┐
    │ 4. HTTP 요청    │ ────────────▶ │ LED 1: RED                      │
    │    전송         │               │ → 소켓 전송 오류                │
    └────────┬────────┘               └─────────────────────────────────┘
             │ 성공
             ▼
    ┌─────────────────┐
    │ 5. HTTP 응답    │
    │    수신         │
    └────────┬────────┘
             │
    ┌────────┴────────┬─────────────────┬─────────────────┐
    │                 │                 │                 │
    ▼                 ▼                 ▼                 ▼
┌────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
│200 OK  │      │401 Unauth│      │404 Not   │      │기타 에러 │
│ICY 200 │      │인증 실패 │      │Found     │      │          │
└────┬───┘      └────┬─────┘      └────┬─────┘      └────┬─────┘
     │               │                 │                 │
     ▼               ▼                 ▼                 ▼
┌────────┐      ┌──────────┐      ┌──────────┐      ┌──────────┐
│LED:    │      │LED: RED  │      │LED: RED  │      │LED: RED  │
│GREEN   │      │ID/PW오류 │      │마운트    │      │재시도    │
│연결성공│      │          │      │포인트오류│      │          │
└────────┘      └──────────┘      └──────────┘      └──────────┘
```

#### 7.2.2 NTRIP 연결 실패 상세 진단표

| 실패 단계 | LED 상태 | 에러 메시지 (로그) | 원인 | 해결 방법 |
|-----------|----------|-------------------|------|-----------|
| 큐 생성 | RED → 태스크 종료 | "GGA 전송 큐 생성 실패" | 메모리 부족 | 디바이스 재시작 |
| 소켓 생성 | RED | "TCP 소켓 생성 실패" | GSM 상태 비정상 | GSM 재초기화 |
| TCP 연결 | RED | "TCP 연결 실패" | 서버 주소/포트 오류, 네트워크 불안정 | 설정 확인, 네트워크 점검 |
| HTTP 전송 | RED | "HTTP 요청 전송 실패" | 소켓 오류 | 재연결 대기 |
| HTTP 응답 | RED | "HTTP 응답 수신 실패" | 서버 무응답, 타임아웃 | 서버 상태 확인 |
| 401 응답 | RED | "인증 실패 - ID/PW 확인 필요" | NTRIP ID/PW 오류 | BLE로 ID/PW 재설정 |
| 404 응답 | RED | "마운트포인트를 찾을 수 없음" | 마운트포인트 이름 오류 | 마운트포인트 재확인 |

#### 7.2.3 NTRIP 재연결 메커니즘

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      NTRIP 재연결 로직 상세                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [정상 동작 중]  LED 1: GREEN                                           │
│       │                                                                 │
│       ▼                                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ 데이터 수신 타임아웃 발생                                        │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│       │                                                                 │
│       ├── 1회차 타임아웃 → LED 1: YELLOW (대기)                        │
│       │                                                                 │
│       ├── 2회차 타임아웃 → LED 1: YELLOW (대기)                        │
│       │                                                                 │
│       └── 3회차 타임아웃 → LED 1: RED (재연결 시작)                    │
│                │                                                        │
│                ▼                                                        │
│       ┌─────────────────────────────────────────────────────────┐      │
│       │ 소켓 강제 종료 (tcp_close_force)                         │      │
│       │ 대기: 500ms                                              │      │
│       └─────────────────────────────────────────────────────────┘      │
│                │                                                        │
│                ▼                                                        │
│       ┌─────────────────────────────────────────────────────────┐      │
│       │ 재연결 시도 (ntrip_connect_to_server)                    │      │
│       │ - TCP 연결 (최대 3회)                                    │      │
│       │ - HTTP 요청/응답                                         │      │
│       └─────────────────────────────────────────────────────────┘      │
│                │                                                        │
│       ┌────────┴────────┐                                              │
│       │                 │                                               │
│       ▼                 ▼                                               │
│   [재연결 성공]    [재연결 실패]                                        │
│   LED 1: GREEN    LED 1: RED                                           │
│   타임아웃 리셋   1000ms 후 재시도                                      │
│   GGA 큐 정리                                                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 7.3 GSM 이벤트별 LED 동작 및 복구

| 이벤트 | LED 동작 | 의미 | 자동 복구 동작 |
|--------|----------|------|----------------|
| `GSM_EVT_RDY` | - | 모뎀 준비 완료 | LTE 초기화 시작 |
| `GSM_EVT_INIT_OK` | RED → YELLOW | LTE 초기화 성공 | NTRIP 태스크 생성 |
| `GSM_EVT_INIT_FAIL` | RED 유지 | LTE 초기화 실패 | 수동 재시작 필요 |
| `GSM_EVT_TCP_CLOSED` | RED | TCP 연결 종료 | NTRIP 재연결 시도 |
| `GSM_EVT_PDP_DEACT` | YELLOW | PDP 컨텍스트 비활성화 | APN부터 재초기화 |
| `GSM_EVT_POWERED_DOWN` | - | 모뎀 전원 OFF | - |

#### 7.3.1 PDP 컨텍스트 비활성화 복구 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   PDP_DEACT 발생 시 복구 절차                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  GSM_EVT_PDP_DEACT 수신                                                │
│       │                                                                 │
│       ▼                                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ 1. LED 1: YELLOW 설정                                            │   │
│  │ 2. ntrip_should_restart = true 설정                              │   │
│  │ 3. lte_reinit_from_apn() 호출 (APN부터 재초기화)                 │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│       │                                                                 │
│       ▼                                                                 │
│  APN 재설정 → 네트워크 재등록 → GSM_EVT_INIT_OK                        │
│       │                                                                 │
│       ▼                                                                 │
│  NTRIP 태스크 재생성 → LED 1: GREEN                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

### 7.4 LED 1 상태별 빠른 진단 가이드

#### 7.4.1 LED 1: OFF (꺼짐)

| 상황 | 원인 | 확인 사항 |
|------|------|-----------|
| 디바이스 시작 직후 | 정상 (GSM 시작 전) | 잠시 대기 |
| 수동좌표 모드 | 정상 (NTRIP 미사용) | 설정 확인 |
| Averaging 완료 후 | 정상 (NTRIP 종료됨) | Base Mode 동작 중 |
| 비정상 종료 | NTRIP 태스크 크래시 | 로그 확인, 재시작 |

#### 7.4.2 LED 1: RED (빨강) - 지속

| 지속 시간 | 가능한 원인 | 진단 방법 | 해결 방법 |
|-----------|-------------|-----------|-----------|
| 시작 후 5초 이내 | GSM 초기화 중 (정상) | 대기 | - |
| 시작 후 10~30초 | AT 통신 실패 | 로그 확인 | 모뎀 전원/연결 확인 |
| 시작 후 30초~1분 | SIM 카드 문제 | CPIN 로그 확인 | SIM 재삽입 |
| 시작 후 1분~2분 | 네트워크 등록 실패 | CREG 로그 확인 | 위치 이동, 안테나 확인 |
| 시작 후 2분 이상 | NTRIP 연결 반복 실패 | 서버 응답 로그 | 서버 설정 확인 |

#### 7.4.3 LED 1: RED (빨강) - 점멸

| 상황 | 의미 | 상태 |
|------|------|------|
| 시작 직후 | GSM 데이터 수신 중 | 정상 (초기화 진행 중) |
| 연결 후 갑자기 | 연결 끊김 후 재연결 시도 | 네트워크 불안정 |

#### 7.4.4 LED 1: YELLOW (노랑)

| 상황 | 의미 | 예상 전환 |
|------|------|-----------|
| GSM 초기화 후 | NTRIP 서버 연결 시도 중 | GREEN (성공) 또는 RED (실패) |
| GREEN에서 전환 | 데이터 타임아웃 (1~2회) | GREEN (데이터 수신) 또는 RED (3회 타임아웃) |
| PDP_DEACT 후 | 네트워크 재등록 중 | GREEN (복구 성공) |

#### 7.4.5 LED 1: GREEN (초록)

| 상황 | 의미 | 주의 사항 |
|------|------|-----------|
| 안정적 점등 | NTRIP 정상 연결, 데이터 수신 중 | 정상 상태 |
| 간헐적 YELLOW | 일시적 타임아웃 | 네트워크 상태 모니터링 |

---

### 7.5 NTRIP 설정 오류 진단

#### 7.5.1 서버 설정 확인 체크리스트

| 항목 | 설정 명령어 | 확인 명령어 | 예시 |
|------|-------------|-------------|------|
| 서버 URL | SC (Set Caster) | - | `SC+rtk2go.com:2101` |
| 마운트포인트 | SM (Set MountPoint) | - | `SM+SEOUL_REF` |
| ID | SI (Set ID) | GI (Get ID) | `SI+myuser` |
| Password | SP (Set Password) | GP (Get Password) | `SP+mypass` |

#### 7.5.2 일반적인 NTRIP 설정 오류

| 증상 (LED/로그) | 원인 | 해결 |
|-----------------|------|------|
| 401 Unauthorized | ID 또는 PW 틀림 | BLE로 ID/PW 재설정 |
| 404 Not Found | 마운트포인트 이름 오류 | 마운트포인트 재확인 |
| TCP 연결 실패 | URL 또는 포트 오류 | 서버 주소:포트 재확인 |
| 연결 성공 후 즉시 끊김 | 서버측 인증 문제 | 서버 관리자 문의 |

---

### 7.6 소켓 상태 모니터링 (디버깅용)

시스템은 10초마다 자동으로 소켓 상태를 확인합니다.

#### 7.6.1 소켓 상태 코드

| 상태 코드 | 의미 | LED 영향 |
|-----------|------|----------|
| 0 (Initial) | 초기 상태 | - |
| 1 (Opening) | 연결 시도 중 | YELLOW |
| 2 (Connected) | 연결됨 | GREEN |
| 3 (Listening) | 수신 대기 | - |
| 4 (Closing) | 연결 종료 중 | RED로 전환 예정 |

#### 7.6.2 AT 커맨드 응답 실패 경고

| 연속 실패 횟수 | 의미 | 조치 |
|----------------|------|------|
| 1~2회 | 일시적 지연 | 자동 재시도 |
| 3회 이상 | 모뎀 응답 없음/데드락 의심 | 시스템 점검 필요 |

---

### 7.7 정상 동작 시 예상 LED 패턴

#### BASE 정상 동작 (NTRIP 사용 + RTK Fixed)
```
LED 1: GREEN (점등)     - NTRIP 연결됨
LED 2: YELLOW (점멸)    - RTK Fixed (BASE는 YELLOW)
LED 3: GREEN (점멸)     - RTCM 송신 중
```

#### ROVER 정상 동작 (RTK Fixed)
```
LED 1: GREEN (점등)     - NTRIP 연결됨 (사용 시)
LED 2: GREEN (점멸)     - RTK Fixed (ROVER는 GREEN)
LED 3: GREEN (점멸)     - RTCM 수신 중
```

#### BASE 정상 동작 (수동좌표 모드)
```
LED 1: OFF              - NTRIP 미사용
LED 2: GREEN (점멸)     - Manual Position 설정됨
LED 3: GREEN (점멸)     - RTCM 송신 중
```

---

### 7.8 문제 상황별 체크리스트

#### 7.8.1 "LED 1이 계속 RED입니다"

```
□ 1. SIM 카드가 제대로 삽입되어 있는가?
□ 2. SIM 카드에 데이터 요금제가 활성화되어 있는가?
□ 3. 안테나가 제대로 연결되어 있는가?
□ 4. 현재 위치가 통신 음영 지역이 아닌가?
□ 5. NTRIP 서버 주소/포트가 정확한가?
□ 6. NTRIP ID/PW가 정확한가?
□ 7. 마운트포인트 이름이 정확한가?
□ 8. NTRIP 서버가 정상 운영 중인가?
```

#### 7.8.2 "LED 1이 GREEN ↔ YELLOW 반복됩니다"

```
□ 1. 네트워크 신호 강도가 약한가? (위치 이동 고려)
□ 2. NTRIP 서버가 불안정한가? (다른 서버 시도)
□ 3. 데이터 요금제 한도를 초과했는가?
□ 4. 주변에 전파 간섭 요소가 있는가?
```

#### 7.8.3 "LED 1이 YELLOW에서 진행되지 않습니다"

```
□ 1. 10~20초 이상 대기해 보았는가? (연결 시도 중)
□ 2. GSM 초기화가 완료되었는가? (이전에 RED 점멸 확인)
□ 3. 로그에서 "NTRIP 서버 연결 시도" 메시지가 보이는가?
□ 4. 방화벽이 해당 포트를 차단하고 있지 않은가?
```

---

## 부록 A. LED 관련 소스 코드 위치

| 파일 경로 | 내용 |
|-----------|------|
| `lib/led/led.h` | LED API 정의 |
| `lib/led/led.c` | LED 하드웨어 드라이버 |
| `modules/gps/gps_app.c` | GPS LED 제어 (LED 2) |
| `modules/gsm/ntrip_app.c` | NTRIP LED 제어 (LED 1) |
| `modules/gsm/gsm_app.c` | GSM/LTE LED 제어 (LED 1) |
| `modules/lora/lora_app.c` | LoRa LED 제어 (LED 3) |
| `modules/gps/base_auto_fix.c` | BASE 모드 상태 LED 모니터링 |

---

## 부록 B. 관련 명령어 (BLE 프로토콜)

| 명령어 | 설명 | 응답 |
|--------|------|------|
| RS (Reset) | 디바이스 재시작 | "Device Reset" + "\n" |
| GG (Get GPS) | 수동 좌표 읽어오기 | "Get (수동좌표)" + "\n" |
| SG (Set GPS) | 수동 좌표 입력 | "Set (수동좌표) Complete" + "\n" |

---

< 끝 >

페이지 1~10
